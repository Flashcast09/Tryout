            <nav class="navbar navbar-expand-lg col-12">
                <div class="entete">
                    <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon fa-solid fa-dumbbell derouler"></span>
                        
                    </button>
                    <div class="imagetitre"><img src={{asset('images/logozyzz(2).jpg')}} alt="">
                {# <p> We're all gonna make it brah</p> #}
                    <a class="navbar-brand" href="{{ path('main') }}">ZYZZ PROJECT</a> 
                    </div>
                
                    
                    <div class="collapse navbar-collapse collapsemenu" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="{{ path('main') }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('categories_index') }}">Boutique</a>
                            </li>
                            <li class="nav-link">
                                <a class="nav-link" href="{{ path('sub') }}" >
                                    Abonnement
                                </a>
                            </li>
                        </ul>
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('profile_index')}}">Mon profil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('cart_index')}}">Mon panier</a>
                            </li>
                             <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout')}}">Me déconnecter</a>
                            </li>
                        {% else %}
                            <li class="nav-item nav-item1">
                                <a class="nav-link " href="{{ path('app_register')}}">M'inscrire</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login')}}">Me connecter</a>
                            </li>
                        {% endif %}
                             
                             
                        </ul>
                         {# <form class="d-flex">
                            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                            <button class="btn btn-outline-success" type="submit">Search</button>
                        </form>  #}
                    </div>
                </div>
            </nav>